import { useRouter } from "next/router"
import React from "react";
import { getStaticProps } from ".";

const PostDetail = () => {
  const router = useRouter();
  const postId = getStaticProps;
  console.log(postId);
    return
  }


 export default PostDetail